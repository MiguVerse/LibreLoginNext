name: "LibreLoginNext"
authors: ["miguvt", "kyngs"]
version: '${version}'
main: "xyz.miguvt.libreloginnext.paper.PaperBootstrap"
api-version: "1.19"

dependencies:
  server:
    Geyser-Spigot:
      load: "BEFORE"
      required: false
    floodgate:
      load: "BEFORE"
      required: false
    LuckPerms:
      load: "BEFORE"
      required: false